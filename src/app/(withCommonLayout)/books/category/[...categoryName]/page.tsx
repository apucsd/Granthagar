const CategoryPage = ({ params }: { params: any }) => {
  console.log(params.categoryName);
  return (
    <div>
      <h2>Welcome to the CategoryPage CategoryPage </h2>
    </div>
  );
};

export default CategoryPage;
